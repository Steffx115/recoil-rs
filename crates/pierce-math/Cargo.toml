[package]
name = "pierce-math"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic fixed-point math types for the Pierce RTS engine"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
